namespace Enums;

public enum ApplicationStatus
{
    Init,
    Applied,
    Interviewing,
    Offer,
    Rejected,
    Accepted
}

public enum MenuSelect
{
    Init,
    AddApplication,
    ShowAllApplications,
    FilterByStatus,
    SortByDate,
    ShowStatistics,
    UpdateStatus,
    RemoveApplication,
    ExitApplication,
}